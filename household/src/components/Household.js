import React from "react";
import styled from "styled-components";

const Household = (props) => {
  return <HouseholdBlock></HouseholdBlock>;
};

const HouseholdBlock = styled.div``;

export default Household;
